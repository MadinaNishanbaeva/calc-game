import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  random: {
    width: 100,
    marginHorizontal: 15,
    marginVertical: 25,
    fontSize: 25,
    textAlign: 'center',
    backgroundColor: '#999',
  },
  disabled: {
    opacity: 0.3,
  },
});
